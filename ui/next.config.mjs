/** @type {import('next').NextConfig} */
const nextConfig = {
    /* config options here */
    // Ensure trailing slashes are handled consistently to avoid 404s
    trailingSlash: false,
};

export default nextConfig;

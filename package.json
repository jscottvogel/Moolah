{
  "name": "moolah-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev -w ui",
    "build": "npm run build -w ui",
    "lint": "npm run lint -w ui",
    "amplify:sandbox": "npx ampx sandbox"
  },
  "workspaces": [
    "ui"
  ],
  "devDependencies": {
    "@aws-amplify/backend": "^1.0.0",
    "@aws-amplify/backend-cli": "^1.0.0",
    "aws-cdk-lib": "^2.114.0",
    "constructs": "^10.0.0",
    "esbuild": "^0.20.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "zod": "^3.25.17"
  }
}
